rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All business reads/writes are performed server-side with Firebase Admin SDK.
    // Firebase Admin bypasses Firestore Security Rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
